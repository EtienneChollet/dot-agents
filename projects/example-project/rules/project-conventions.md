# Project Conventions

Project-specific rules go here. These are symlinked to
`<project>/.claude/rules/` and loaded alongside global rules.

Use project rules for conventions that only apply to this project,
not globally. For example:

- Project-specific naming conventions
- Architecture constraints
- Data format requirements
- Domain-specific terminology
